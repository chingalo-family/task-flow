import 'package:flutter/material.dart';

class TaskListContainer extends StatelessWidget {
  const TaskListContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('list container'),
    );
  }
}
